# frozen_string_literal: true

RSpec.describe Filedictrb do
  it "has a version number" do
    expect(Filedictrb::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
